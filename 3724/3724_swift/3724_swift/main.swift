//
//  main.swift
//  3724_swift
//
//  Created by Hyun on 2022/11/06.
//

import Foundation

let t = Int(readLine()!)!
for _ in 0..<t{
    let nm = readLine()!.split(separator: " ").map{Int($0)!}
    let n = nm[0]
    let m = nm[1]
    var map = Array(repeating: Array(repeating: 0, count: m), count: n)
    for i in 0..<n{
        for k in 0..<m{
            
        }
    }
}

