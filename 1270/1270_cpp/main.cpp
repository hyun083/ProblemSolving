#include <iostream>
#include <map>
#include <algorithm>
using namespace std;

int main() {
    int n;
    cin >> n;
    for(int T=0; T<n; T++){
        int size;
        cin >> size;
        map<int,int> map;
        for(int i=0; i<size; i++){
            if(map.find(cin)){
                
            }
        }

    }
    return 0;
}
